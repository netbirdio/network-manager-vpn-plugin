[keyfile]
# NetBird owns its WireGuard interfaces. NetworkManager must not configure them.
# Default NetBird interfaces use the wt prefix. If you configure a custom
# interfaceName/interface-name outside this pattern, add that name here too.
unmanaged-devices=interface-name:wt*
